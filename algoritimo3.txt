Algoritmo de cálculo de média: um algoritmo que calcula a média de uma lista de 
números.

1.leia a lista;
2.leia o tamanho_lista
3.soma = 0;
4.para cada elemento da lista faça:
	soma = elemento + soma;
5.media = soma/tamanho_lista
6.escreva media;
